# Perplexity MCP Server 环境变量配置
# 复制此文件为 .env 并填入实际值

# ============================================
# MCP 服务配置
# ============================================

# MCP 服务端口
MCP_PORT=8000

# MCP API 认证密钥 (客户端需要在 Authorization header 中携带此密钥)
MCP_TOKEN=sk-123456

# ============================================
# Perplexity 账户凭证 (可选)
# 用于解锁高级功能: Pro 模式、Reasoning 模式、Deep Research
# 不配置则只能使用 auto 模式
# ============================================

# 从 Perplexity 网站 Cookie 中获取
# 打开 perplexity.ai -> F12 开发者工具 -> Application -> Cookies
PPLX_NEXT_AUTH_CSRF_TOKEN=12345
PPLX_SESSION_TOKEN=12345

# ============================================
# SOCKS 代理配置 (可选)
# 如果需要通过代理访问 Perplexity，配置此项
# 不配置则直接连接
# ============================================

# SOCKS5 代理格式: socks5://[user[:pass]@]host[:port][#remark]
# 示例:
#   socks5://127.0.0.1:1080
#   socks5://user:password@proxy.example.com:1080
#   socks5://user:password@127.0.0.1:1080#my-proxy
# 留空或不配置表示不使用代理
SOCKS_PROXY=

